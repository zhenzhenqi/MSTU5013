<editor>
    // Hello The Original Adam
    <h2>EDITOR: Hello { opts.username }!</h2>

    // Adam Smith says Bitcoin is the bomb.
    <p>{ opts.editTweet.author } says { opts.editTweet.content }</p>

    // Bitcoin logo image
    <img src={ opts.editTweet.imageURL }>
</editor>
